{
  "name": "ai-clinical-scribe",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "bash tools/init.sh",
    "build:libs": "pnpm --filter @scribe/shared-types --filter @scribe/ai run build",
    "dev": "pnpm run build:libs && pnpm --parallel --filter \"./apps/*\" run dev",
    "verify": "pnpm run build:libs && pnpm -r run lint && pnpm -r run typecheck && pnpm -r run test && pnpm -r run build",
    "test:e2e": "pnpm --filter api run test:e2e && pnpm --filter web run test:e2e",
    "db:migrate": "pnpm --filter api run db:migrate",
    "db:seed": "pnpm --filter api run db:seed",
    "icd10:embed": "pnpm --filter api run icd10:embed"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.67.0"
  },
  "packageManager": "pnpm@11.13.1"
}
